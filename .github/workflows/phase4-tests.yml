name: Phase 4 Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  phase4-foundation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run Phase 4 foundation tests
        run: node --test tests/phase4-foundation.test.js
